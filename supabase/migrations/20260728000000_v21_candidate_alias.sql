-- ════════════════════════════════════════════════════════════════════════
-- v21 — 다음 책 후보에 '추천인 별명' 추가 (2026-07-28)
--
-- 배경: 투표 화면의 추천자 표기가 'No.7220' 처럼 휴대폰 뒤 4자리 그대로였다.
--   번호는 재미도 없고 열람증 번호가 그대로 노출된다. 추천할 때 원하는 별명을
--   직접 붙일 수 있게 하고, 비워두면 앱이 우주 테마 닉네임을 자동 배정한다
--   (자동 닉은 클라이언트 결정론 해시라 DB 저장 없음 — alias 는 직접 적었을 때만 채워짐).
--
-- 설계: next_book_candidate 에 alias 컬럼 추가(선택 입력, 빈 문자열 기본, 20자 상한).
--   기존 테이블 단위 grant/RLS 로 신규 컬럼도 커버됨(컬럼 단위 grant 아님).
-- ════════════════════════════════════════════════════════════════════════

alter table next_book_candidate
  add column if not exists alias text not null default '' check (char_length(alias) <= 20);
