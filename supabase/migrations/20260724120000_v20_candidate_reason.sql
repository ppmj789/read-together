-- ════════════════════════════════════════════════════════════════════════
-- v20 — 다음 책 후보에 '추천 이유' 추가 (2026-07-24)
--
-- 배경: 후보로 책을 올릴 때 "왜 이 책을 같이 읽고 싶은지" 한 줄을 붙일 수 있게.
--   제목·저자만으론 투표 판단 근거가 약했다.
--
-- 설계: next_book_candidate 에 reason 컬럼 추가(선택 입력, 빈 문자열 기본).
--   기존 테이블 단위 grant/RLS 로 신규 컬럼도 커버됨(컬럼 단위 grant 아님).
-- ════════════════════════════════════════════════════════════════════════

alter table next_book_candidate
  add column if not exists reason text not null default '' check (char_length(reason) <= 500);
