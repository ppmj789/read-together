# Supabase CLI 설정 — GitHub 연동/마이그레이션 자동 적용용
# project_id 는 라벨일 뿐, 실제 적용 대상은 GitHub Secrets 의 PROJECT_REF.
# 필요 시 `supabase init` 으로 전체 옵션을 재생성할 수 있습니다.
project_id = "read-together"

[db]
major_version = 15

[api]
enabled = true

[studio]
enabled = true
